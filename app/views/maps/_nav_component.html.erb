<nav class="navbar fixed-top navbar-expand-lg navbar-dark">
  <div class="container-fluid">
    <a href='/'>
      <div class="logoImage"></div>
    </a>
    <!--&nbsp;
    <%= link_to 'Location Bookmarker', root_path, class: 'navbar-brand' %>-->

    <!--<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>-->

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <!--<div class="form-inline my-0 my-lg-0 form-label-group">
            <select class="form-control-lg custom-select" id="searchType">
              <option value="">Address search</option>
              <option value="">Location search</option>
            </select>

            <!--
            <label for="searchLocation">Latitude, Longitude</label>
            <input class="form-control" id="searchLocation" type="search" placeholder="Latitude, Longitude" aria-label="Search" required="required">
            -->
            <!--
            <input class="form-control" id="searchLocation" type="text" placeholder="Latitude, Longitude" aria-label="Search" required>

            <button class="btn btn-success mr-5" id="locationSearch">
              <i class="fas fa-search"></i> Search
            </button>
          </div>-->
          <a href="#" class="nav-link" id="reduce_url_link">Reduce URL</a>
        </li>
        <li class="nav-item">
          <a href="#how_it_works" class="nav-link" id="how_it_works_link">How it works</a>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href='' id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-user"></i>
              <%= current_user.username.blank? ? current_user.email : current_user.username %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to profile_path, class: 'dropdown-item' do %>
                <span class="icon">
                  <i class="fas fa-user"></i>
                </span>
                <span>Profile</span>
              <% end %>

              <%= link_to bookmarks_path, class: 'dropdown-item' do %>
                <span class="icon">
                  <i class="fas fa-bookmark"></i>
                </span>
                <span>Bookmarks</span>
              <% end %>

              <%= link_to destroy_user_session_path,  method: :delete, class: 'dropdown-item' do %>
                <span class="icon">
                  <i class="fas fa-sign-out-alt"></i>
                </span>
                <span>Logout</span>
              <% end %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_session_path %>">
              <!--<i class="fas fa-sign-in-alt"></i>--> Sign in
            </a>
          </li>
          <li class="nav-item mr-5">
            <a class="nav-link" href="<%= new_user_registration_path %>">
              <!--<i class="fas fa-user-plus"></i>--> Sign up
            </a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
